import { BaseGeneratedListTypes, KeystoneConfig } from '@keystone-next/keystone/types';
import { AuthConfig } from './types';
/**
 * createAuth function
 *
 * Generates config for Keystone to implement standard auth features.
 */
export declare function createAuth<GeneratedListTypes extends BaseGeneratedListTypes>({ listKey, secretField, initFirstItem, identityField, magicAuthLink, passwordResetLink, sessionData, }: AuthConfig<GeneratedListTypes>): {
    withAuth: (keystoneConfig: KeystoneConfig) => KeystoneConfig;
};
