/// <reference types="node" />
import { IncomingMessage, ServerResponse } from 'http';
import { GraphQLSchema } from 'graphql';
import type { KeystoneConfig, CreateContext } from '../../types';
export declare const createExpressServer: (config: KeystoneConfig, graphQLSchema: GraphQLSchema, createContext: CreateContext) => Promise<{
    expressServer: import("express-serve-static-core").Express;
    apolloServer: import("apollo-server-express").ApolloServer<{
        req: IncomingMessage;
        res: ServerResponse;
    }>;
}>;
